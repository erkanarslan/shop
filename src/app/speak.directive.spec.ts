import { SpeakDirective } from './speak.directive';

describe('SpeakDirective', () => {
  it('should create an instance', () => {
    const directive = new SpeakDirective();
    expect(directive).toBeTruthy();
  });
});
